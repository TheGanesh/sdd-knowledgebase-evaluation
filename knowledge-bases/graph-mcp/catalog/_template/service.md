# <service-name> — narrative

<What aggregate/data this service owns and its place in the system.>

## Business rules
- <rule the LLM must respect that isn't visible in the contracts>

## Integration gotchas
- <latency coupling, transactional boundaries, schema-contract caveats>

> Machine-readable detail lives in `catalog-info.yaml`, `openapi.yaml`, `asyncapi.yaml`.
> This file is only the "why" a human needs.
