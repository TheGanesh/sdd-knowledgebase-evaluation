{
  "id": "KB-205",
  "pattern": "single-service cache (precision)",
  "services": ["inventory-service"],
  "apis": ["inventory-service GET /api/inventory/{sku}"],
  "topics": [],
  "datastores": ["cache:sku-availability"],
  "rationale": "A cache-config change internal to inventory-service. No contract or event change reaches order-service. Over-claiming order-service is a precision error."
}
